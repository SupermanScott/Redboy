Tests for Views
How to tie into expiration? Pass that into View.append and Mirror._save_internal?
Decide if the Key class can DIE!
Add in Unique Indices using Hash. something like user:email -> <email1> = <user_record_key>
